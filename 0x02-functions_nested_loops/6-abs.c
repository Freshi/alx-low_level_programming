#include "main.h"

/**
 * _abs - Returns absolute value of number
 * @i: number to convert to absolute
 *
 * Description: Returns the absolute value of a given int
 * Return: 0;
 */

int _abs(int i)
{
	if (i < 0)
	{
	return (-i);
	}
	else
	{
	return (i);
	}
}
