#include <stdio>

/**
 * main - Printns alphabet
 *
 * Description: prints alphabet in upper case
 * Return: 0
 */

int main(void)
{
	for (int x = 'A'; x <= 'Z'; x++)
	{
		putchar(x);
	}

	putchar("\n);

	return (0);
}
